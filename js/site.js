/** Production origin for OG / sitemap. Update after custom domain. */
export const SITE_ORIGIN = 'https://universum-sliver.vercel.app';

/** GA4 Measurement ID (gtag in each HTML <head>). Same account can verify Search Console. */
export const GA_MEASUREMENT_ID = 'G-S3N7T50JZN';

/** Japan manhole-card sister site (prefecture deep links). */
export { MANHOLE_ORIGIN } from './manhole.js';
