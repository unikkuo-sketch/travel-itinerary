### 2026-08-26 — Google Search Console（Phase 0）

- 問題：分享／SEO 要先能量測收錄；Search Console 尚未驗證、sitemap 未提交
- 曾考慮：HTML 檔或 meta 驗證；網域資源 + DNS TXT；Search Console API 自動提交
- 放棄原因：`*.vercel.app` 無法加 DNS TXT；API／HTML 檔都仍要站長 Google 帳號；站上已有 GA4 gtag，多一種驗證檔多餘
- 現行方案：
  - 操作說明 `docs/gsc-setup.md`：URL 前綴 `https://universum-sliver.vercel.app/` + **Google Analytics** 驗證（同一帳號、`G-S3N7T50JZN` 已在首頁 `<head>`）
  - 驗證後手動提交 `sitemap.xml`；抽樣要求編入索引
  - `npm run check-gsc` 打正式站確認 robots／sitemap／gtag／各 URL 200（不含登入）
  - HTML 檔僅作 GA 失敗時備用（放入 `public/google*.html` 並部署）
- 驗證：`npm run check-gsc` 顯示 Ready；站長在 GSC 完成驗證與 sitemap 後，資源狀態為已擁有、sitemap 成功
