### 2026-07-31 — SEO Phase 1＋2 實作

- 問題：行程頁 CSR 空殼、OG 通用、agent 缺發現入口
- 曾考慮：整站 Next SSR；僅改 query meta 不改 path
- 放棄原因：SSR 過重；path＋build 預渲染即可解 crawler／分享
- 現行方案：
  - Phase 1：`llms.txt`／`llms-full.txt`、sitemap（path＋lastmod、排除 shopping）、canonical、shopping `noindex`、`js/seo.js`
  - Phase 2：正式 URL `/trips/{id}/`（stories／food 同目錄）；`prerender-trips.mjs` 寫入 meta＋overview／days／章節正文；cover 進 OG；`vercel.json` 舊 `?trip=` → 301
  - build：`gen-seo` → vite → prerender；dev 中間件映 path → 頁殼
- 驗證：`npm run build`；curl 預渲染 HTML 含行程 title／正文；legacy query 仍可由 getTripId 解析
